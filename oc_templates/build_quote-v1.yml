apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: quote-v1
  namespace: nldemo
spec:
  output:
    to:
      kind: ImageStreamTag
      name: 'quote:v1'
  source:
    git:
      ref: database
      uri: 'https://github.com/snowjet/demo-quote-gen.git'
    type: Git
  strategy:
    type: Source
    sourceStrategy:
      from:
        kind: ImageStreamTag
        name: 'python:latest'
        namespace: openshift
      env: []
  triggers:
    - type: ImageChange
      imageChange: {}
    - type: ConfigChange
